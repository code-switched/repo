# TODO List

## Improvements

- [ ] Move git init below "This is the last step." Wait until user confirms steps before initializing the repo.
- [ ] Implement this trick `trick.md` using `readline` to have path aware entry for user input
- [ ] Make script aware of the username the user entered so we dont log out if gh auth is already logged into the correct username.